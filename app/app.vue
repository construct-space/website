<script setup>
// Initialize theme on app load
const { initTheme } = useAppTheme()
onMounted(() => {
  initTheme()
})

useHead({
  meta: [
    { name: 'viewport', content: 'width=device-width, initial-scale=1' }
  ],
  link: [
    { rel: 'icon', href: '/favicon.ico' }
  ],
  htmlAttrs: {
    lang: 'en'
  }
})

const title = 'CONSTRUCT - Build Apps Visually'
const description = 'The all-in-one platform for building modern applications. Code, design, deploy - all in one place with AI assistance.'

useSeoMeta({
  title,
  description,
  ogTitle: title,
  ogDescription: description,
  twitterCard: 'summary_large_image'
})
</script>

<template>
  <UApp>
    <NuxtLayout>
      <NuxtPage />
    </NuxtLayout>
  </UApp>
</template>
